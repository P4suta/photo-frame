[files]
extend-exclude = [
    "target",
    "node_modules",
    "**/pkg",
    "**/dist",
    "*.otf",
    "*.ttf",
    "*.jpg",
    "*.jpeg",
    "*.png",
    "*.webp",
    "*.lock",
]

[default]
# Also lint filenames — a typo in a module file or component name
# would otherwise propagate through every import that references it.
check-filename = true
extend-ignore-re = [
    # Hex color literals
    "#[0-9a-fA-F]{3,8}\\b",
]

[default.extend-words]
# Project-specific allow-list:
# (empty for now — add false positives as they arise)
